
Function1(Pin Configuration){
    PIN(1, GND, 0V, Open-Drain(type2));
    PIN(2, VDD, 3.3V, Open-Drain(type2));
    PIN(3, UART, DW, 9600, 8, 0, 1, 0, Open-Drain(type1));
    PIN(4, UART, DR, Open-Drain(type1));
}

Function2(Get GPS Raw Data){
    DR(4, UART, DIRECT), +1s;
}
