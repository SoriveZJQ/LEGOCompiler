 
Function1(Pin Configuration){
    PIN(1, VDD, 3.3V, Open-Drain(type2));
    PIN(2, GND, 0V, Open-Drain(type2));
    PIN(3, VDD, 3.3V, Open-Drain(type1));
    PIN(4, ADC, 5V);
}

Function2(Data Computation){
    DF(Y1, UV Intensity,( (5/pow(2,16))*(X1<<8+X2)-1)*7.5, mW/cm2);
}

Function3(Read Analog Output){
    DR(4, ADC,X1, X2), +1ms;
}